# [프로젝트 설명 매뉴얼] SCU 스토리보드(SB) 자동 검수 시스템

> 본 문서는 동료, 유관부서, 교수자, 외주 개발사 등 다양한 이해관계자에게 **`SB-Review-Automation` 프로젝트의 가치와 기능, 활용법을 명확하고 설득력 있게 설명**할 수 있도록 구성된 공식 커뮤니케이션 가이드입니다.

---

## 1. 30초 엘리베이터 피치 (핵심 요약)

> **"교수님의 원고와 외주업체 스토리보드(SB)를 AI가 1분 만에 정밀 대조하여, 규격 검사부터 원고 누락/왜곡 점검, 그리고 파워포인트 메모(Comment) 삽입까지 한 번에 완료해 주는 지능형 교육 콘텐츠 품질 보증 솔루션입니다."**

- **기존**: 원고와 SB를 두 화면에 띄우고 수십 페이지를 한 줄씩 눈으로 대조 (과목당 1~2시간 소요, 단순 오탈자·서식 규격 놓침 빈번)
- **도입 후**: 파일만 넣고 "SB 검수해줘" 한마디면 **마크다운 종합 보고서**와 **피드백 메모가 자동 주입된 PPTX**가 즉시 생성되어 검수 시간을 **90% 이상 단축**

---

## 2. 왜 이 프로젝트를 만들었는가? (배경 및 문제의식)

### 🔴 AS-IS (기존 실무의 고질적 페인포인트)
1. **극심한 수작업 피로도**: 한 주차당 30~50장에 달하는 슬라이드를 원고와 일일이 대조하느라 본질적인 교수설계 품질(교육적 효과성)을 볼 시간이 부족함.
2. **기계적 규격 오류의 잦은 누락**: 폰트 크기(본문 20pt Bold), 슬라이드 영역 이탈, 이미지 출처 표기 누락 등 반복적인 규칙 위반을 사람이 다 잡아내기 어려움.
3. **피드백 작성의 번거로움**: 검수한 내용을 다시 엑셀이나 메신저, PPT 메모로 일일이 옮겨 적어 외주사에 전달하는 이중 작업 발생.
4. **학습정리 작성의 공백**: 교수 원고에 학습정리가 누락되어 있을 경우, 설계자가 내용을 새로 요약·작문해야 하는 병목 현상 발생.

### 🟢 TO-BE (본 시스템 도입 효과)
- **정량적 규격 검사는 기계가 1초 만에**, **내용적 논리 검수는 고성능 LLM이 정밀하게** 분담하는 **2-Track 하이브리드 검수 체계** 구축.
- 외주 개발사에 즉시 전달 가능한 **'메모가 달린 수정 PPTX'**가 자동으로 만들어져 피드백 전달 공수가 제로화됨.
- 누락된 학습정리는 슬라이드 본문 맥락을 분석하여 **'AI 추천 완성 문안'**을 즉시 생성해 제공.

---

## 3. 핵심 아키텍처 및 차별점 (How it Works)

본 시스템은 단순한 텍스트 비교 도구가 아닌, **서울사이버대학교의 공식 제작 지침을 완벽히 내재화한 2-Track 하이브리드 구조**입니다.

```
                  [ 원고 (HWP / PPTX 등) ] + [ SB 초안 (PPTX) ]
                                      │
               ┌──────────────────────┴──────────────────────┐
               ▼                                             ▼
     [ Track 1: 정량 검사 (Python) ]            [ Track 2: 정성 검수 (LLM Agent) ]
     - 슬라이드 16:9 규격 검증                   - 원고 대조 내용 왜곡/누락 감지
     - 본문 최소 폰트(20pt Bold) 확인            - 교수설계 논리 (학습목표, 퀴즈)
     - 화면 텍스트 이탈(오버플로우) 감지        - 누락된 학습정리 완성 문안 자동 생성
     - 이미지 출처 표기 여부 전수 스캔          - 렌더링 이미지 시각 교차 검증 (오탐 방지)
               │                                             │
               └──────────────────────┬──────────────────────┘
                                      ▼
                      [ 실무 최적화 1쌍(Pair) 최종 산출물 ]
            1. 종합 마크다운 보고서 (.md) - 4대 표준 항목
            2. 검수 메모 주입 파워포인트 (.pptx) - 외주사 즉시 전달용
```

### 💡 4대 핵심 기술적 특징
1. **정량 + 정성 2-Track 하이브리드 파이프라인**: 폰트나 규격은 파이썬이 수학적 팩트로 잡고, 문맥 왜곡이나 교육적 정합성은 AI가 분석합니다.
2. **시각 교차 검증 (Visual Cross-Check)**: 텍스트 추출 시 생길 수 있는 오탐(예: 퀴즈 번호 ①, ② 누락 오인)을 방지하기 위해, 슬라이드를 실제 이미지로 렌더링하여 눈으로 직접 확인하는 교차 검증을 거칩니다.
3. **1쌍(Pair) 산출물 디폴트 원칙**: 검수 결과가 텍스트 보고서에 그치지 않고, `python-pptx` 오픈XML 파싱을 통해 **파워포인트 네이티브 메모(Comment)**로 해당 슬라이드마다 꽂혀 나옵니다.
4. **엄격한 Read-Only LCDMS 연동**: 서울사이버대 LCDMS 시스템에서 원고와 SB를 안전하게 자동 수집하며, 어떠한 데이터도 시스템에 쓰거나 변경하지 않는 무결성을 보장합니다.

---

## 4. 보고서 4대 표준 항목 구성

검수 보고서는 실무진이 한눈에 파악할 수 있도록 표준화된 4개 영역으로 제공됩니다:

| 항목 | 점검 내용 |
| :--- | :--- |
| **1. 총평** | 지적사항 합계 표(내용/레이아웃/저작권) 및 전체적인 품질에 대한 종합 권고사항 |
| **2. 내용 검수 결과** | 원고 대조 내용 누락·왜곡, 학습목표 행동동사, 퀴즈 정답/해설 오류, **AI 추천 학습정리 문안** |
| **3. 레이아웃 검수 결과** | 슬라이드 16:9 규격(33.867x19.05cm), 본문 20pt Bold 위반, 화면 경계 이탈(오버플로우), 도해화 제안 |
| **4. 저작권 및 출처 점검** | 본문 이미지 대비 슬라이드 노트 출처(`#1`, 게티이미지 번호 등) 미기재 슬라이드 일괄 추출 |

---

## 5. 대상(청중)별 맞춤 설명 가이드

### 👤 A. 내부 동료 및 콘텐츠 기획/검수 담당자에게 설명할 때
> "검수할 때 원고랑 SB 두 개 켜두고 눈 빠지게 비교하느라 시간 많이 쓰셨죠? 이 시스템에 파일 넣고 실행하면 규격 오류는 파이썬이 다 긁어주고, 누락되거나 왜곡된 문장은 AI가 짚어줍니다. 특히 **외주업체에 보낼 피드백 메모가 PPTX에 자동으로 달려서 나오기 때문에**, 우리는 생성된 메모만 훑어보고 바로 전달하면 됩니다."

### 👤 B. 외주 개발사 / 스토리보드 작가에게 설명할 때
> "저희가 이번에 도입한 자동 검수 시스템은 설계를 지적하기 위한 것이 아니라, **누락이나 서식 오류를 사전에 필터링해 재작업(리테이크)을 최소화하기 위한 품질 보증 도구**입니다. 전달드리는 PPTX 파일에 구체적인 수정 가이드가 메모 형태로 달려 있으니, 해당 슬라이드에서 바로 수정 작업을 진행하실 수 있습니다."

### 👤 C. 팀장 / 의사결정권자에게 보고할 때
> "기존 1개 주차당 평균 90분 이상 소요되던 SB 1차 검수 시간을 **10분 이내로 단축**시키는 자동화 시스템입니다. 단순 서식 검수는 100% 자동화하고, AI가 학습정리 문안까지 직접 작성해 주므로 검수 인력의 리소스를 고차원적인 교육 설계 품질 고도화에 집중시킬 수 있습니다."

---

## 6. 자주 묻는 질문 (FAQ)

**Q1. AI가 원고를 잘못 해석해서 엉뚱한 지적(오탐)을 하지는 않나요?**
> **A.** 본 시스템은 단순 프롬프트 방식이 아니라, 슬라이드를 실제 PNG 이미지로 렌더링하여 텍스트 데이터와 시각 레이아웃을 교차 검증하는 로직을 갖추고 있습니다. 또한 SCU의 실제 설계 예외 규칙(예: 소형 아이콘 출처 면제, `#1` 출처 앵커 허용, 우측 '화면설명' 영역 검수 제외 등)이 룰베이스로 정교하게 필터링되어 오탐률이 극히 낮습니다.

**Q2. 파워포인트 메모가 달린 파일은 어떤 형태인가요?**
> **A.** 파워포인트를 열었을 때 우측 '검토 > 메모' 패널에 지적사항이 깔끔하게 표시됩니다. 검수관이 직접 파워포인트를 열고 일일이 댓글을 단 것과 완전히 동일한 표준 XML 메모 형태로 생성됩니다.

**Q3. LCDMS 시스템에 로그인해서 파일을 받는데, 보안상 안전한가요?**
> **A.** 시스템 연동 모듈(`lcdms_downloader.py`)은 **엄격한 Read-Only(단방향 조회/다운로드)**로만 동작하도록 설계되어 있습니다. 시스템의 데이터를 수정·삭제·등록하는 쓰기 작업은 원천적으로 배제되어 있어 안전합니다.

---

## 7. 실무 사용법 (Step-by-Step 워크플로우)

누구나 순서대로 따라 하면 즉시 검수를 완료할 수 있는 단계별 실무 가이드입니다.

```
[1단계: 다운로드 세팅] ──> [2단계: input/ 파일 배치] ──> [3단계: "SB검수해줘"] ──> [4단계: output/ 1쌍 산출물 확인]
  (setting/ 파일 설정)      (원고 + SB 2개 파일)          (2-Track 자동 검수)      (마크다운 보고서 + 메모 PPTX)
```

### 1단계: LCDMS 자료 자동 다운로드 세팅 (최초 1회 및 세션 갱신)
LCDMS에서 원고와 SB를 손으로 일일이 다운받지 않고 자동 수집하려면 아래 2가지만 설정합니다:

1. **과목 URL 등록 (`setting/contents_set_url.md`)**:
   - 검수하려는 과목의 LCDMS 개발현황 페이지 URL을 한 줄로 등록합니다.
   - 예시: `- 머신러닝 : https://home.iscu.ac.kr/cdms/progress/devstatu/devstatuDetailSView.scu?contents_seqno=173620...`
2. **로그인 세션 쿠키 등록 (`setting/lcdms_cookie.txt`)**:
   - 웹 브라우저(Chrome 등)에서 LCDMS에 로그인합니다.
   - `F12(개발자 도구)` ➔ `Application(애플리케이션)` 탭 ➔ 좌측 `Cookies` ➔ `https://home.iscu.ac.kr` 클릭
   - `tongken` 항목의 Value 값을 복사하여 `setting/lcdms_cookie.txt`에 저장합니다. (형식: `tongken=165397fd8f...`)
   - *(※ 쿠키가 만료되면 "권한 없음" 알림이 뜨므로, 이때 브라우저에서 최신 tongken 값을 다시 복사해 넣으면 됩니다.)*
3. **다운로드 실행**:
   - 대화창에 **"머신러닝 12주차 자료 다운로드 해줘"**라고 요청하거나 터미널에서 실행:
     ```bash
     python SB-Design/scripts/lcdms_downloader.py 머신러닝 12
     ```
   - 시스템이 원고와 SB 초안을 자동으로 판별하여 `input/` 폴더에 즉시 저장합니다.

---

### 2단계: 검수 대상 자료 위치 (`input/`)
검수를 실행하기 전, 검수할 대상 파일 2개가 **`input/` 폴더**에 있어야 합니다:

- 📂 **위치**: `input/` 폴더
- 📄 **필수 자료 (1개 주차당 1쌍)**:
  1. **교수 원고 파일** (1개): `.hwp`, `.hwpx`, `.docx`, `.pdf`, `.pptx` (예: `머신러닝_12.pptx`)
  2. **외주사 SB 초안 파일** (1개): `.pptx` (예: `SB_머신러닝_12_v1.0_260914.pptx`)
- *(※ 1단계 LCDMS 다운로더를 사용하면 `input/`에 자동으로 들어오며, 수동으로 다운받은 파일이 있다면 `input/` 폴더에 직접 드래그앤드롭해 넣어도 무방합니다.)*

---

### 3단계: 자동 검수 실행
자료가 `input/` 폴더에 준비되었다면, 에이전트에게 한마디만 요청하면 됩니다:

- 💬 **실행 명령어**: 대화창에 **"SB검수해줘"** (또는 "머신러닝 12주차 검수해줘")
- *(터미널 직접 실행 시: `python SB-Design/run_sb_audit.py`)*
- 실행 즉시 파이썬 정량 검사(Track 1)와 AI 문맥 검수(Track 2)가 순차적으로 자동 가동됩니다.

---

### 4단계: 검수 산출물 확인 (`output/`)
검수가 완료되면 **`output/` 폴더**에 항상 **동일한 파일명의 산출물 1쌍(Pair)**이 생성됩니다:

- 📂 **위치**: `output/` 폴더
- 📦 **최종 산출물 (1쌍 기본 원칙)**:
  1. **최종 마크다운 보고서** (`output/SB_검수결과_<파일명>_YYMMDD_HHMM.md`):
     - **1. 총평**: 지적사항 건수 요약표 및 종합 품질 권고사항
     - **2. 내용 검수 결과**: 원고 대조 누락/왜곡, 퀴즈 정오답 오류, **AI 추천 학습정리 완성 문안**
     - **3. 레이아웃 검수 결과**: 16:9 규격, 폰트 20pt Bold 위반, 화면 텍스트 이탈(오버플로우)
     - **4. 저작권/출처 점검**: 본문 이미지 대비 슬라이드 노트 출처 누락 목록
  2. **검수메모 반영 파워포인트** (`output/SB_검수결과_<파일명>_YYMMDD_HHMM.pptx`):
     - 위 보고서의 지적사항이 **파워포인트 네이티브 '메모(Comment)'** 형태로 각 슬라이드마다 꽂혀 있는 파일입니다.
     - **외주 개발사나 작가에게 그대로 전달**하여 바로 수정 작업을 진행하게 할 수 있습니다.

---

### 5단계: 임시 파일 정리 및 이력 보존 원칙
- **임시 파일 자동 삭제**: 검수 중 생성된 렌더링 이미지(`rendered_slides/`) 및 중간 덤프 파일은 검수 종료 시 깨끗하게 자동 삭제됩니다.
- **최종 산출물 영구 보존**: `output/`에 생성된 완성본(`.md` 및 `.pptx`)은 이전 차수 검수 이력(History)이므로 삭제되지 않고 안전하게 영구 보존됩니다.


